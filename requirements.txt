# =============================================================================
# Learning the Levers - Python Dependencies
#
# The GCP VM (n2-standard-2) has no GPU. Install CPU-only PyTorch first
# to avoid pulling ~5 GB of unused CUDA/nvidia packages:
#
# Install (two steps):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# =============================================================================

# --- Core: Scraping & Browser Automation ---
selenium>=4.15,<5.0
undetected-chromedriver>=3.5,<4.0
python-dotenv>=1.0,<2.0

# --- Core: HTTP & Data ---
requests>=2.31,<3.0
numpy>=1.26,<3.0
pandas>=2.1,<4.0

# --- Database Export ---
pyarrow>=14.0,<24.0

# --- Feature Engineering: Text ---
emoji>=2.8,<3.0
fasttext-wheel>=0.9.2,<1.0       # or: pip install fasttext

# --- Feature Engineering: Style ---
textstat>=0.7.3,<1.0
nltk>=3.8,<4.0

# --- Feature Engineering: Embeddings ---
sentence-transformers>=2.2,<6.0   # requires torch – install CPU-only separately (see header)

# --- Feature Engineering: Visual ---
opencv-python-headless>=4.8,<5.0
Pillow>=10.0,<13.0
pytesseract>=0.3.10,<1.0         # also requires: sudo apt install tesseract-ocr

# --- Testing ---
pytest>=7.4,<10.0

# --- Optional ---
# duckdb>=0.9                    # Parquet export verification (not required)
# mediapipe>=0.10                # Enhanced face detection with --use-mediapipe
